/*
classes - estrutura de dados - modelo
definir elementos para ter características das informações
composta a atributos e comportamento
        (variáveis) (função/método)
- posição na memória ram do computado que armazena dados
modelar classe  - amazenar dados

endereço pode ser classe
telefone pode ser string com os (), ddd e o número
colocar cada parte em uma variável

instanciar dar vida a uma classe - atribuir valores
materializa-la - criando um objeto

objeto adquire os valores a paritr do esqueleto(classe)

a cada cliente(classe) preenchendo as fichas - acrescenta/cria um objeto
objeto é uma variável
new - instanciar um objeto a partir da classe
atribui valores as propriedades
variável.Propriedade - objeto1.nome = "Natália"; exemplo
mesmo que os valores da variável sejam iguais
*/

//Apache NetBeans IDE

package com.mycompany.Aula01;

public class Conta {
    public double saldo;
    //suporta maior quantidade de baits de números
    public String nomeCorrentista;    
}
